name: Npm build ⚙️

on:
  pull_request:
    branches: [ dev ]
    paths:
      - "src/assets/**/*"

jobs:
  update-npm-build:
    uses: pressbooks/reusable-workflows/.github/workflows/npm-build.yml@main
