// ref: http://jqueryui.com/tabs/
jQuery(document).ready(function($) {
    $('#tabs').tabs();

    // $('.ui-tabs-panel').hide();
    // $(".ui-tabs-active").removeClass("ui-tabs-active");
    // $(".ui-state-active").removeClass("ui-state-active");
    // $(".ui-tabs-tab").click(function() {
    //     $($(this).attr("href") + " .ui-tabs-panel").first().show();
    // });
});
